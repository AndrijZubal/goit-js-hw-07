// const textTitle = document.querySelector(`#categories`);
// console.log(` в списку ${textTitle.children.length} категорії`);

// textTitle.querySelectorAll(`.item`).forEach((item) => {
//   //   console.log();
//   console.log(`Категорія: ${item.querySelector(`h2`).textContent}`);
//   console.log(
//     `Кількість елементів ${item.querySelector(`ul`).children.length}`
//   );
// });
